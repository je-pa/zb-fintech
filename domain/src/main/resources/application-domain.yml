spring:
  jpa:
    generate-ddl: false
    show-sql: true
    hibernate:
      ddl-auto: validate # 데이터베이스랑 일치하는거 - 잘못된 데이터 입력 방지
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl # 대소문자 구분
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3307/fintech?characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: fintech
    password: zerobase
  redis:
    host: redis
    port: 6379